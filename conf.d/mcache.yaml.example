init_config:

instances:
  - url: mcache  # url used to connect to the memcached instance
    port: 11211 # If this line is not present, port will default to 11211
    tags:
      - role:cache
      - service:memcached
